{
  "name": "my-app",

  "scripts": {
	"dev": "nuxt",
	"build": "nuxt build",
  "prod": "HOST=0.0.0.0 PORT=3000 nuxt start",
	"start": "npm run build && npm run prod",
	"generate": "nuxt generate"
  },

  "dependencies": {
    "nuxt": "^1.4.0"
  }
}
